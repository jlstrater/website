gitPublish {
    repoUri = 'git@github.com:jlstrater/website.git'

    branch = 'gh-pages'

    // what to publish, this is a standard CopySpec
    contents {
        from 'build/docs'
    }

    commitMessage = 'Publishing a new page'
}